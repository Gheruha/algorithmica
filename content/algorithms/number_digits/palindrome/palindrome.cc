/* palindrome.cc
 *  A palindrome string or number is always equal with the opposite of himself
 *  For example: "mom" = "mom", 313 = 313
 *  while "hey" != "yeh", 123 != 321
 * */

// Palindrome algorithm steps:
// 1. Generate a opposite copy of the variable.
// 2. Compare the original variable with the copy.
// 3. If variable == copy -> palindrome, else -> not palindrome

#include <iostream>
#include <string>
using namespace std;

// function for finding string palindromes
string s_palindrome(string s){
  // the copy variable that will keep the opposite:

}

int main() {
  // palindrome variabes
  string s = "mom";
  int n = 313;

  // not palindrome variables
  string s2 = "hey";
  int n2 = 123;

}
